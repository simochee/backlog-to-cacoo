import { defineConfig } from "wxt";

export default defineConfig({
  manifest: {
    name: "Backlog to Cacoo",
    description: "Copy Backlog issues as Cacoo cards",
    permissions: ["activeTab"],
  },
});
