pre-commit:
  parallel: true
  jobs:
    - name: lint
      glob: "*.{ts,tsx,js,jsx}"
      run: npx oxlint --fix {staged_files}
      stage_fixed: true
    - name: fmt
      glob: "*.{ts,tsx,js,jsx,json,css}"
      run: npx oxfmt --write {staged_files}
      stage_fixed: true
